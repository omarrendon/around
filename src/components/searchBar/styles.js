import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  searchBarInput: {
    borderRadius: '5px !important',
    [`& fieldset`]: {
      border: '3px solid #FD8898',
    },
  }
})
